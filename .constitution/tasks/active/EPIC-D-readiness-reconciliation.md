# Epic D: Readiness reconciliation

Close this Stage 4 iteration without crossing an unresolved upstream decision. This epic produces a reconciliation report and routes required changes back to Stage 3.

#### OXY-D001 Reconcile pre-implementation readiness evidence

- **Type:** Chore
- **Effort:** 2
- **Dependencies:** OXY-A007, OXY-B001, OXY-B002, OXY-B003, OXY-B004, OXY-B005, OXY-B006, OXY-B007, OXY-B008, OXY-C005
- **Preconditions:** Re-fetch every source that a spike fetched through the Jina reader proxy directly from its canonical URL before preserving it as a fixture; proxied bodies are never fixture bytes.
- **Category:** Docs
- **Scope (In-Scope Files):**
  - `.constitution/reports/pre-implementation-readiness.md`
- **Scope (Out-of-Scope Files):**
  - `.constitution/tech-spec/` (name the required Stage 3 pass; don't perform it in this ticket)
  - `.constitution/tasks/` (the next Stage 4 version follows upstream reconciliation)
  - Candidate source trees
  - Qualification measurements
- **Verification Command:** `prettier --prose-wrap never --check '.constitution/**/*.md'`
- **Expected Success Output:** `exit 0` and a report that classifies every pre-implementation KU as resolved KK, retained KU, or blocked external input
- **STOP Conditions:**
  - STOP if any result is inferred from a plan, implementation intention, inaccessible hardware, or candidate-internal counter.
  - STOP after naming the exact Stage 3 revisions and remaining user or external inputs; don't set `candidateImplementationReady` from Stage 4.
- **Description:** Consolidate the contract-validator results, six spike recommendations, hardware-access register, assessor confirmations, staged tool and external-contract manifests, baseline tooling, measurement templates, environment tooling, and read-only lock report. Produce an exact Stage 3 reconciliation checklist and the conditions for the next Stage 4 minor release. The checklist must separately name the approved 52-capability baseline, reference application, scenes, interaction scripts, fonts, assets, window matrix, cache states, release flags, scoring anchors, assessor assignments, reference-environment captures, and authoritative resolved-tool lock.

##### OXY-D001 Inputs from Epics A and C

The reconciliation report must name these Stage 3 revisions:

- `.constitution/tech-spec/data-models/capability-traceability.schema.json` `mappings[].contractTests[]` identifies a contract test but has no physical file location.
- `.constitution/tech-spec/data-models/accessibility-map.schema.json` `reverseActions[].textLayoutBinding` has no text-layout generation value.
- `.constitution/tech-spec/data-models/specification-phase.schema.json` `promotionEvidence.layoutQualification`, `finalContractSet`, `targetMatrix`, `losingCandidateRemoval`, and `billOfMaterials` use generic evidence references instead of typed schemas.
- `.constitution/tech-spec/data-models/raw-measurement.schema.json` omits the `$schema` property, so raw-measurement instances cannot self-declare their schema.
- `.constitution/tech-spec/data-models/raw-measurement.schema.json` doesn't state that `samples[].monotonicNs` is non-decreasing per `(constraintId, launch)`.
- No Stage 3 schema defines `qualification-lock.schema.json#measurementPolicy.sampleValidityRules`; `qualification/schemas/sample-validity.schema.json` is the proposed staged schema and its digest is the proposed binding value.
- The proposed external-contract lock values in `qualification/schemas/external/proposed-external-contract-lock.json` await Stage 3 adoption.
- `xtask environment inspect` writes the `PATH.inventory.json` companion artifact, but no Stage 3 schema defines it and `qualification-lock.schema.json#referenceEnvironments` has no typed reference to it.
- Wayland interface-set completeness has no Stage 3 rule. The companion inventory retains a partial observed `protocolVersion`, which lock v5 cannot represent.
- OXY-D001 must confirm the temporary 256 KiB capture bound for `wayland-info` and `xdpyinfo` against real Ubuntu 26.04 output sizes. Truncated output remains fail-closed.
- `qualification-lock.schema.json#measurementPolicy.{scoringAnchors,assessors,fuzzCorpora,securityPatchRehearsal}` binds path-less digests; the repository convention `qualification/staged/<field>.json` is the proposed referent and needs Stage 3 typing.
- `qualification-lock.schema.json#resolvedTools` deliberately has no `pathRoot`: campaign-host paths and tool records remain only in `qualification/staged/fuzz-corpora.json` `hostToolRecords` and bind through `measurementPolicy.fuzzCorpora`. The proposed `pathRoot` schema addition and campaign-host `resolvedTools` transformation are withdrawn because `xtask/src/toolchain/lock.rs` and `xtask/src/contracts/readiness.rs` reject them.
- Dependency advisory validation is deferred: `cargo deny --offline check advisories` and `cargo audit` can use a host-cached RustSec database, but the repository has no pinned vendored advisory database for CI. CI runs `cargo deny check licenses bans sources` until OXY-D001 records a pinned offline advisory database and its refresh policy.
- The staged native toolchain supports only `x86_64-unknown-linux-gnu`; other Tier 1 hosts are an OXY-D001 lock input.
- Platform and accessibility baselines are validated by `contracts validate`. Workload, scoring-anchor, corpus, and sample-validity baseline validation have no owning command; `measurement validate` covers sample-validity records only. Stage 3 and Stage 4 must assign owners for the remaining baseline validation.
- Template generation command surface remains deferred until the measurement-harness contract exists. The `generate_templates` and `compute_comparison_bounds` library APIs remain the interim interface.

##### OXY-D001 Inputs from Epic B

The reconciliation report must name these Stage 3 revisions, retained gating known unknowns, and blocked external inputs. Each bullet cites the section of the Epic B deliverable that produces it.

- Reconcile the Wayland and X11 GTK protocol rows: [SPK-B003, `Spec edits required`](../../spikes/SPK-B003.md#spec-edits-required) and [SPK-B004, `Spec edits required`](../../spikes/SPK-B004.md#spec-edits-required) both propose the Ubuntu `libgtk-4-1` `4.22.2+ds-1ubuntu1` package identity. OXY-D001 must confirm that both rows remain aligned and keep the `gtk4` crate `v4_20` API-binding ceiling distinct from the reference package identity.
- Reconcile the Wayland and X11 AT-SPI protocol rows: [SPK-B003, `Spec edits required`](../../spikes/SPK-B003.md#spec-edits-required) proposes the Ubuntu `at-spi2-core` `2.60.0-1` package identity for Wayland with cited upstream 2.60.6 XML source-floor evidence, and [SPK-B004, `Spec edits required`](../../spikes/SPK-B004.md#spec-edits-required) proposes `at-spi2-core` `2.60.0-1` for X11. OXY-D001 must confirm that both rows remain aligned and keep the upstream 2.60.x XML source floor distinct from the reference package identity.
- Reconcile external-fixture `source.json` sidecar ownership: [SPK-B003, `Spec edits required`](../../spikes/SPK-B003.md#spec-edits-required) is the sole authority for the 10 Wayland fixtures, and [SPK-B004, `Spec edits required`](../../spikes/SPK-B004.md#spec-edits-required) is the sole authority for the 15 X11 fixtures. Both follow the Epic C convention in [`qualification/schemas/external/README.md`](../../../qualification/schemas/external/README.md): `path` and `licenseSource.path` are upstream-repository-relative, and source-file sidecars use an SPDX-only `license`. Ubuntu package-page captures use `LicenseRef-page-copyright-notice`, `licenseSource` pointing at `https://ubuntu.com` and `legal/terms-and-policies/intellectual-property-policy`, `licenseUrl` `https://ubuntu.com/legal/terms-and-policies/intellectual-property-policy`, and the identical required `licenseNote`; every numbered edit quotes the commit-pinned `retrievalUrl` that its sidecar table records.
- Route new domain terms to the Stage 1 glossary at `.constitution/prd/glossary.md` before Stage 3 adopts them: `ordinary visit` and `attempted ordinary visits` ([SPK-B005, `Question`](../../spikes/SPK-B005.md#question)); `layout prequalification suite` ([SPK-B005, `Layout prequalification additions inventory`](../../spikes/SPK-B005.md#layout-prequalification-additions-inventory)); `second-configuration score-4 evidence` ([OXY-B007 access register, `Purpose and scope`](../../reports/reference-hardware-access.md#purpose-and-scope)); `semantic-role registry` ([SPK-B001, `Stage 3 semantic-role decision and P2R registry freeze`](../../spikes/SPK-B001.md#stage-3-semantic-role-decision-and-p2r-registry-freeze)); `authorship independence` ([OXY-B008 assessor record, `Independence rules`](../../reports/qualification-assessors.md#independence-rules)); and the `display-epoch equality tuple`, including its `targetModeSignature` component, as a refinement of the glossary's existing `Display epoch` entry ([SPK-B002, `B002-WIN-10 two-stream, two-display procedure`](../../spikes/SPK-B002.md#b002-win-10-two-stream-two-display-procedure)).

Each spike specifies its KU delta in isolation. OXY-D001 must compose `KNOWN_UNKNOWN_BINDINGS` from all deltas (B002 +5, B003 +11, B004 +0, B005 +1, B006 -1). B004's 10 retained gates add no string because the existing `minimum-platform-and-protocol-versions`, `complete-ime-editing-geometry-and-accessibility-maps`, `independent-presentation-opportunity-sources`, and `capability-and-platform-baselines` strings already cover them. Update the three distinct exact-set assertions before running `test-all`: `xtask/src/commands/lock_tests.rs` `committed_candidate_gate_is_valid_but_open_with_the_exact_ku_set`, which derives from committed `qualification-lock.json`, applies all deltas and changes 13 to 29 (`13 + 5 + 11 + 0 + 1 - 1`); `xtask/src/commands/lock_tests.rs` `cleared_ku_without_evidence_remains_open_with_the_exact_remaining_ku_set` and `crates/oxyflut-qualification/src/readiness.rs` `clearing_a_ku_string_without_its_evidence_keeps_the_gate_open`, which derive from `qualification/fixtures/readiness/cleared-without-evidence.json`, apply only B005 +1 and B006 -1 and each remains 12 (`12 + 1 - 1`). OXY-D001 must also compose the final `schema_count` and `instance_count` pair for the `xtask/src/contracts/schema.rs` test assertions from the per-spike deltas: SPK-B001 +2 schemas and +1 contract instance, SPK-B005 +3 schemas, changing 18/6 today to 23/7; after all header edits, OXY-D001 also regenerates the generated-binding golden once because SPK-B001 is the only spike that touches the header.

**Stage 3 revisions required by each spike**

- `.constitution/spikes/SPK-B001.md`, [Spec edits required](../../spikes/SPK-B001.md#spec-edits-required): four atomic macOS deployment-target retentions (`.constitution/tech-spec/stack.md` `Platform qualification pins` macOS row, `.constitution/tech-spec/contracts/platform-contracts.json` `environments.macos.minimumVersion` and `environments.macos.openQuestions`, and `.constitution/tech-spec/contracts/qualification-lock.json` `referenceEnvironments.macos-arm64.minimumVersion`), the accessibility-KU guard on `complete-ime-editing-geometry-and-accessibility-maps`, and the D0 semantic-role prerequisite that creates `.constitution/tech-spec/data-models/semantic-role-registry.schema.json`, `.constitution/tech-spec/data-models/semantic-role-registry-snapshot.schema.json`, `.constitution/tech-spec/contracts/semantic-role-registry.json`, and the two schema-fixture directories `qualification/fixtures/contracts/semantic-role-registry/` and `qualification/fixtures/contracts/semantic-role-registry-snapshot/`; migrates `.constitution/tech-spec/data-models/accessibility-map.schema.json` from `urn:oxyflut:schema:accessibility-map:5` to `:6` with keyed `forward.roles`; generates `SemanticRole` and `OXY_SEMANTICS_ROLE_*` constants in `oxyflut-public.rs`, `oxyflut-substrate.rs`, and `oxyflut-substrate.h`; advances `.constitution/tech-spec/contracts/oxyflut-substrate.h` `OXY_SUBSTRATE_ABI_VERSION` from `10u` to `11u`, mirrors ABI 11 in `qualification/fixtures/native/interface.json`, renames `xtask/src/contracts/native_tests.rs` `abi_seven_through_nine_fail_before_callbacks_install` to `abi_seven_through_ten_fail_before_callbacks_install` and changes its rejected range from `7..=9` to `7..=10`, and regenerates `qualification/fixtures/native/layout.x86_64-unknown-linux-gnu.json` through the macro-based `qualification/fixtures/native/layout-probe.c.in` after the header update with `"abiVersion":11`, reviewing every size, alignment, offset, and nullability value; regenerates `qualification/fixtures/generated-bindings/oxyflut-substrate.rs` and `qualification/fixtures/generated-bindings/oxyflut-substrate.rs.sha256` using the pinned bindgen and `sha256sum` commands after the header edit, reviews the generated ABI-11 constant and every `OXY_SEMANTICS_ROLE_*` constant, and requires `contracts validate` to byte-compare the golden and its digest sidecar; adds the `CAP-SEM-001` and `CAP-SEM-002` bindings in `.constitution/tech-spec/contracts/capability-traceability.json`; removes `"roles"` from `xtask/src/contracts/traceability/edges.rs` `REQUIRED_ACCESSIBILITY_CATEGORIES`; adds the `contracts/semantic-role-registry.json` physical contract edge for `CAP-SEM-001` and `CAP-SEM-002`; adds the generated `SemanticRole`, `OXY_SEMANTICS_ROLE_*`, and registry-pointer symbol edges for those capabilities; and records the breaking migration note in `.constitution/tech-spec/changelog.md`.
- `.constitution/spikes/SPK-B002.md`, [Spec edits required](../../spikes/SPK-B002.md#spec-edits-required): the source-fixture capture procedure that must run before any Windows status promotion, then the ten numbered edits, which set `environments.windows.timing.{status,interactiveOpportunitySource,independentMeterSource,perDisplayAssociation,evidence}` and `environments.windows.recoveryBaseline.{status,path,sha256}` in `.constitution/tech-spec/contracts/platform-contracts.json`; append five new strings to both `.constitution/tech-spec/contracts/qualification-lock.json` known-unknown arrays; add five `KNOWN_UNKNOWN_BINDINGS` rows in `crates/oxyflut-qualification/src/readiness.rs`; update the exact `known_unknowns` set in `xtask/src/commands/lock_tests.rs`; and append a `Decision` paragraph to `.constitution/tech-spec/adrs/ADR-0005-platform-hosts.md`.
- `.constitution/spikes/SPK-B003.md`, [Spec edits required](../../spikes/SPK-B003.md#spec-edits-required): the replacement `environments.wayland.protocols` array, including the `wl_display` and `wl_registry` rows, and the `environments.wayland.{minimumVersion,openQuestions,ime.numericNegotiation,timing.interactiveOpportunitySource,timing.independentMeterSource,timing.presentationFeedback,timing.perDisplayAssociation,accessibilityMaps,recoveryBaseline,allocations.focused,allocations.integrated}` values in `.constitution/tech-spec/contracts/platform-contracts.json`; the 29-evidence-object capture map; the replacement Wayland row in `.constitution/tech-spec/stack.md` `Platform qualification pins`; the eleven new strings, including `wayland-drm-vblank-calibration-acceptance-bound`, in `.constitution/tech-spec/contracts/qualification-lock.json`; the acceptance-bound prose belongs in the schema-defined `environments.wayland.timing.independentMeterSource` string field; eleven `KNOWN_UNKNOWN_BINDINGS` rows in `crates/oxyflut-qualification/src/readiness.rs`; the exact 24-element `known_unknowns` set in `xtask/src/commands/lock_tests.rs`; and the appended `Consequences` text in `.constitution/tech-spec/adrs/ADR-0005-platform-hosts.md`.
- `.constitution/spikes/SPK-B004.md`, [Spec edits required](../../spikes/SPK-B004.md#spec-edits-required): the nine numbered edits that set the X11 row in `.constitution/tech-spec/stack.md` `Platform qualification pins`, set `environments.x11.reference`, the `GTK`, `X Present`, and `AT-SPI` protocol rows, `environments.x11.timing.presentationFeedback`, `environments.x11.timing.independentMeterSource`, and `environments.x11.ime.{evidence,status,numericNegotiation}` in `.constitution/tech-spec/contracts/platform-contracts.json`, retain `environments.x11.minimumVersion` and `environments.x11.status` as `ku-gating`, and set `referenceEnvironments.x11-linux-x86_64.operatingSystem` while retaining its `minimumVersion` and `systemPackageLockDigest` as `null` in `.constitution/tech-spec/contracts/qualification-lock.json`.
- `.constitution/spikes/SPK-B005.md`, [Spec edits required](../../spikes/SPK-B005.md#spec-edits-required): the new layout contracts and the atomic qualification-lock v5-to-v6 migration described in the layout bullet below.
- `.constitution/spikes/SPK-B006.md`, [Spec edits required](../../spikes/SPK-B006.md#spec-edits-required): create `qualification/staged/fuzz-corpora.json` and `qualification/staged/security-patch-rehearsal.json` from the canonical bytes and bind their digests in `.constitution/tech-spec/contracts/qualification-lock.json` `measurementPolicy.fuzzCorpora` and `measurementPolicy.securityPatchRehearsal`; use a `licenses` array for every corpus set and `maxLenBytes` for every ingress; retain campaign-host tool records only in `qualification/staged/fuzz-corpora.json` `instrumentation.campaignToolchain.hostToolRecords`, bound through `measurementPolicy.fuzzCorpora`. Non-reference campaign-host tool records with `reference: false` are an explicit exemption from the register's prohibition on adding `thinkpadp14s` hardware, GPU, driver, or access information to the qualification lock because they identify a toolchain rather than a qualification environment; each new campaign host re-binds `measurementPolicy.fuzzCorpora`. The proposed `resolvedTools` transformation and `pathRoot` schema edit are withdrawn because `xtask/src/toolchain/lock.rs` and `xtask/src/contracts/readiness.rs` reject them; after admission passes, remove `fuzz-corpora` and `security-patch-rehearsal` and add `campaign-host-tool-records` to both known-unknown arrays (a net reduction of one); replace their two `KNOWN_UNKNOWN_BINDINGS` rows in `crates/oxyflut-qualification/src/readiness.rs` with `campaign-host-tool-records`, bound to `measurementPolicy.fuzzCorpora` with evidence path `qualification/staged/fuzz-corpora.json` and upstream owner `OXY-D001`; remove the two policy KUs and add `campaign-host-tool-records` to all four KU arrays in `qualification/fixtures/readiness/invalid.json` and `qualification/fixtures/readiness/cleared-without-evidence.json`; and update the exact KU sets in `xtask/src/commands/lock_tests.rs`.

**Gating known unknowns each spike retained**

- SPK-B001 `Decision register`: 12 gating KUs against 6 KK rows — the macOS deployment target, the input method editor transcript, the numeric and sensitive-field policy, both allocation accessibility maps, timing-observer independence, display migration, both allocations' service routing, both allocations' recovery injection, and immutable evidence publication.
- SPK-B002 `Question` table: 20 gating KUs and 0 KK rows — the minimum Windows build, SDK installation, Win32 and TSF interface fixtures, ACP UTF-16 conversion, input scopes, UI Automation provider baseline and maps, integrated inventory, per-output timing and variable refresh, acknowledgement source, service routing, topology and resize inputs, DXGI device-loss signals, device-loss, resize, topology, and surface-loss recovery, and immutable evidence.
- SPK-B003 Table 1: 12 gating KUs against 9 KK rows — the Ubuntu compositor session and package lock, the GTK release floor on the reference, the IME operation-unit transcript, the AT-SPI scalar-to-`TextIndex::Logical` representation, the AT-SPI text, caret, selection, and editable transcript, both allocation accessibility maps, the independent presentation-opportunity meter, both allocations' service routing, and both allocations' recovery injection.
- SPK-B004 Table 1: 10 gating KUs against 4 KK rows — the native X server and extension floor, the IBus GTK transcript, the Orca version-pinned assistive-technology documentation, both allocation AT-SPI maps, the independent timing meter and per-output association, both allocations' interface inventory and service routing, and both allocations' recovery injection.
- SPK-B005 Table 1 row 6: 1 gating KU against 5 KK rows — `measurementPolicy.layoutVisitCap` stays `null` and `layout-visit-cap` stays in both known-unknown arrays until the bounded 48-tuple timing probe supplies schema-valid evidence under `CON-PERF-001`. The 48-tuple probe is gated on [the blocked macOS arm64 and Windows x86-64 hardware rows `B007-Q01` and `B007-Q02`](../../reports/reference-hardware-access.md#answers), which are listed below under `Hardware register blocked rows`; it isn't actionable until both rows are unblocked.
- SPK-B006 Table 1 row 1: 1 gating KU against 5 KK rows — no disclosed upstream engine patch applies to every frozen Flutter line and both consumption paths, because the candidate `08da33b` libpng fix is already incorporated by all three resolved engine pins.

**Hardware register blocked rows**

- `.constitution/reports/reference-hardware-access.md`, `Answers` and `Reference conformance and feasibility`: macOS arm64 and Windows x86-64 are BLOCKED — B007-Q01, B007-Q02, B007-Q05, B007-Q06, B007-Q09, and B007-Q10 have no named owner, access procedure, reference comparison, or distinct second configuration. `Spec edits required` authorizes no Stage 3 edit, and the register explicitly forbids adding `thinkpadp14s` to the qualification lock. Non-reference campaign-host tool records with `reference: false` at `qualification/staged/fuzz-corpora.json` `instrumentation.campaignToolchain.hostToolRecords` are an explicit exemption because they identify a toolchain, not a qualification environment, and carry no GPU, driver, or access data; each new campaign host re-binds `measurementPolicy.fuzzCorpora`.
- `.constitution/reports/reference-hardware-access.md`, `Reference conformance and feasibility`: the CONFIRMED Wayland x86-64 and X11 x86-64 rows are one non-reference NixOS 26.05 machine and supply no second-configuration score-4 evidence, so `hardware-gpu-driver-and-system-package-locks` remains an open lock input.

**Assessor record and authorship-independence governance**

- `.constitution/reports/qualification-assessors.md`, `Question` row Q4 and `Second-assessor confirmation procedure`: no distinct second human is named, available, or confirmed. This is the OXY-B008 stop condition and a blocked external input, not a technical gap. `measurementPolicy.assessors` stays `null` and `scoring-anchors-and-two-assessors` stays in both known-unknown arrays.
- `.constitution/reports/qualification-assessors.md`, `Assessor 1 independence determination` and `Spec edits required`: Stage 1 must first approve and apply the exact `.constitution/prd/constraints.md` `Substrate selection policy` sentence forbidding a candidate's implementation or evidence author from serving as its independent scorer; only afterward may Stage 3 update the conforming qualification contracts. Until then assessor 1's disclosed authorship is a gating conflict whose only representable mitigation is whole-candidate replacement.
- `.constitution/reports/qualification-assessors.md`, `Schema representability and recusal` and `Spec edits required`: the per-criterion recusal option is a blocked future Stage 3 full qualification-evidence v5-to-v6 migration inventory, not a standalone schema edit. It changes `.constitution/tech-spec/data-models/qualification-evidence.schema.json` root `assessors`, `$defs.assessorScore`, `$defs.score.properties.assessorScores`, `$defs.score.required`, and `properties.recusedAssessorIds`; advances its identity and `schemaVersion`; updates supersession, direct-schema and production-readiness fixture corpora and their digest dependents; and adds migration, semantic-validator, Rust-identity, README, and changelog changes. It requires Stage 1 approval first.
- `.constitution/reports/qualification-assessors.md`, `Question` row Q2: the scoring anchors are unfrozen, so `measurementPolicy.scoringAnchors` stays `null` and the anchor workshop needs two accepted assessors before it can run.

**Layout-visit contracts, schemas, and version migration proposed by SPK-B005**

- `.constitution/spikes/SPK-B005.md`, `Durable corpus bytes` and `Layout prequalification additions inventory`: create `qualification/staged/layout-visit-corpus.json` with SHA-256 `4972e43333984047b5a1d84200d5b89a29c5b59e47c5aca8773379320f2c6c84`, `qualification/staged/layout-visit-counting-rules.json` with SHA-256 `6cd0d7c7b06587525d9127f15cceecdd6f9c21b8a62be93c70c9b3756ca459c2`, `.constitution/tech-spec/data-models/layout-qualification-record.schema.json` with SHA-256 `09d96af49384e47ee6154f386af2ef771985516a61c843d561835654283bd7b1`, `.constitution/tech-spec/data-models/layout-prequalification-run.schema.json` with SHA-256 `76dfee7dfcdfdd49e2d67afdf83ab43c29dbb6513652a8023b0869a7d59293e2`, and `.constitution/tech-spec/data-models/layout-prequalification-suite.schema.json` with SHA-256 `27e3a876f3b8d5e88ad43089a9eff0c7ce225a6d9cece5fcd789f7759c05c924`.
- `.constitution/spikes/SPK-B005.md`, [Spec edits required](../../spikes/SPK-B005.md#spec-edits-required): add `layoutVisitCorpus`, `layoutQualificationRecordSchema`, `layoutPrequalificationRunSchema`, `layoutPrequalificationSuiteSchema`, `layoutVisitCountingRules`, and `layoutPrequalificationIdentities` to `$defs.measurementPolicy` and `$defs.resolvedMeasurementPolicy` in `.constitution/tech-spec/data-models/qualification-lock.schema.json`, migrate `.constitution/tech-spec/contracts/qualification-lock.json` `schemaVersion` from `5.0.0` to `6.0.0` with those six bound fields, and create `.constitution/tech-spec/migrations/qualification-lock-v5-to-v6.md`. `sampleValidityRules` and `layoutVisitCap` stay `null`, both readiness flags stay `false`, and `layout-visit-cap` stays in the known-unknown arrays.
- `.constitution/spikes/SPK-B005.md`, `Version migration inventory`: 56 active-version-dependent paths under `xtask/` and `qualification/` must move from `"0.15.0"` to `"0.16.0"`, classified `R`, `R+D`, and `R+N`, together with `.constitution/tech-spec/guidelines.md` (both `v0.15.0` literals and the layout-prequalification validation-command row), `.constitution/tech-spec/stack.md` `Version`, the Scope guard references, and the `specificationVersion` fields in `specification-phase.json`, `platform-contracts.json`, and `capability-traceability.json`, plus the `v0.16.0` entry prepended to `.constitution/tech-spec/changelog.md`.
- `.constitution/spikes/SPK-B005.md`, [Spec edits required](../../spikes/SPK-B005.md#spec-edits-required): the public and qualification contract changes — `LayoutResult.attempted_ordinary_visits` and its migration note in `oxyflut-public.rs`, `LayoutTransactionCounters` and `CandidateProbe::run_layout_fixture` in `oxyflut-qualification.rs`, the `urn:oxyflut:schema:qualification-lock:6` identifier in `xtask`, the new `layout-prequalification validate` command, and the new validator and schema fixture sets.
- `.constitution/spikes/SPK-B005.md`, `Next bounded probe` and `Sample-validity policy`: the unscored 48-tuple prequalification probe profile (2 candidates x 4 environments x 6 passing fixtures, 20 launches, 300 warmup and 500 measured frames) and the no-exclusion sample-validity rule that requires `measurementPolicy.sampleValidityRules` to be exactly `null` for this probe. Stage 3 must also append the unscored-probe allowance to the `.constitution/tech-spec/stack.md` Scope guard.
- Route `.constitution/prd/constraints.md` `Performance and resource constraints` paragraph beginning "The numeric common-case node-visit limit" to Stage 1: replace its gating condition with "The numeric common-case node-visit limit for CAP-LAY-001 remains a gating known unknown until the prequalification lock binds candidate and environment identities and the 48-tuple timing probe supplies schema-valid evidence under CON-PERF-001 on unblocked reference hardware." The corpus is created by the SPK-B005 Stage 3 edits, so its existence cannot remain the condition.
- Route `.constitution/architecture/risks.md` `ARC-R02` `Mitigation or follow-up` to Stage 2: record the corpus and Table 4 finite per-policy visit-cap freeze as partially discharged; retain the numeric global layout-visit cap as the remaining gating condition until the prequalification lock binds candidate and environment identities and the 48-tuple timing probe supplies schema-valid evidence under CON-PERF-001 on unblocked reference hardware.

**Fuzz-corpus and security-patch staged inputs and toolchain pin from SPK-B006**

- `.constitution/spikes/SPK-B006.md`, `Canonical staged inputs`: `qualification/staged/fuzz-corpora.json` has SHA-256 `59d3459130a585e335df491f464258f40b3708c48d237600df960722ffcda105` (15,932 bytes) and `qualification/staged/security-patch-rehearsal.json` has SHA-256 `27b6e4525723e2501d08e72169f8194bb070d4a79b32825f3cc70fe9e66fc14c` (1,991 bytes). Both bind `measurementPolicy.fuzzCorpora` and `measurementPolicy.securityPatchRehearsal`, whose current values are `null`.
- `.constitution/spikes/SPK-B006.md`, `Recommendation` and `Synthetic patch and expected result`: the shared rehearsal is the predeclared synthetic patch `OXY-SYN-SEC-001` with the four frozen post-patch tests `checked_rgba_bytes_accepts_4096_by_4096_rgba`, `checked_rgba_bytes_rejects_4097_by_4096_rgba`, `checked_rgba_bytes_rejects_u32_max_square_without_decoder_or_adapter_call`, and `asset_decode_replays_image_registry`, which the qualification scaffold does not yet define.
- `.constitution/spikes/SPK-B006.md`, `Instrumentation and campaign procedure` and [Spec edits required](../../spikes/SPK-B006.md#spec-edits-required): the campaign toolchain pin is the dated `nightly-2026-08-12` selector resolving commit `3d6c19bb9ab4798ecfb2ee943df01a811720fc27`, with `cargo-fuzz` 0.13.2, GNU Time 1.10 CPU accounting, a 5-second timeout, cumulative process CPU across resumed shards, and an executable-hash preflight. The rolling `nightly` record is inadmissible. For each actual campaign host, Stage 3 must retain the selected tool record only in `qualification/staged/fuzz-corpora.json` `instrumentation.campaignToolchain.hostToolRecords`, bound through `measurementPolicy.fuzzCorpora`; non-reference campaign-host tool records with `reference: false` are an explicit exemption from the register's prohibition because they identify a toolchain rather than a qualification environment, and each new campaign host re-binds `measurementPolicy.fuzzCorpora`; the proposed `resolvedTools` transformation and `pathRoot` schema edit are withdrawn because `xtask/src/toolchain/lock.rs` and `xtask/src/contracts/readiness.rs` reject them. After staged-input admission, replace `fuzz-corpora` and `security-patch-rehearsal` with `campaign-host-tool-records`, whose `KNOWN_UNKNOWN_BINDINGS` row binds `measurementPolicy.fuzzCorpora` to `qualification/staged/fuzz-corpora.json` with upstream owner `OXY-D001`; retain it until every campaign host that produced evidence has a complete selected record. Leave `resolved-tool-digests` as OXY-A008's separate `resolvedTools` gate.
- `.constitution/spikes/SPK-B006.md`, `Frozen corpus sources`: all eight architecture ingress categories map to five immutable source sets with 18 SHA-256-verified seed byte streams and six retained license notices, including both Apache-2.0 and MIT notices for `image` and the `Unicode-3.0` identifier for the Unicode 16.0.0 snapshot.

- **Acceptance:**
  - **Mode:** invariant
  - **Evidence:**

```text
Invariants:
- Every entry in preImplementationKnownUnknowns has one cited result and one owner.
- KK means verified evidence exists; KU means a named unanswered question remains; blocked external input is not relabeled as technical completion.
- The report contains no candidate implementation ticket, measurement result, score, selection, or production plan.
- The report states that this Stage 4 iteration cannot set candidateImplementationReady and identifies every missing approved or captured lock input.
- The next action is either an exact Stage 3 reconciliation pass or an explicit blocked state with required user/external input.
Checker: prettier --prose-wrap never --check '.constitution/**/*.md'
```
