# Fuzzing placeholder

This directory is outside the stable workspace because `cargo-fuzz` generated targets require nightly. It preserves the target repository structure without adding a nightly workspace member.
